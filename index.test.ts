import { OpenBanking } from "./index";

describe("open-banking-account-and-transaction-typescript-sdk", () => {
    it("initialize client", async () => {
        const openbanking = new OpenBanking({
        });
    });
});
